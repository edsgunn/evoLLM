# STATUS: SUPERSEDED — do not submit without checking why.
# Ran as job 6071674. Superseded by node_4room_7b_chr0025 — same crossover
# scheme but at sigma = 0.0025, with refill off. See
# runs/node_4room_7b_chromo_6071674/NOTES.md.
#
# Arm 1 of the reproduction sweep. Identical to node_4room_7b_free40 in every
# respect except the crossover scheme: sites are cut into 3 contiguous
# chromosomes (early / middle / late layers) with one crossover point each,
# so a child switches parent ~4 times instead of ~55. Blocks of interacting
# projections — q beside k, v beside o, layers in residual order — are
# inherited whole, which is the precondition for co-adaptation to ever be
# heritable. Mutation is left exactly as the baseline so this arm isolates
# linkage.
run_name: node-4room-7b-chromo
seed: 0
backend: vllm

model:
  name: Qwen/Qwen2.5-7B-Instruct
  dtype: bfloat16
  max_model_len: auto
  extend_context: true

world:
  rooms:
    - {id: gpu0, gpu: 0, capacity_blocks: 48000}
    - {id: gpu1, gpu: 1, capacity_blocks: 48000}
    - {id: gpu2, gpu: 2, capacity_blocks: 48000}
    - {id: gpu3, gpu: 3, capacity_blocks: 48000}
  edges: complete
  block_size: 16
  eviction: requester
  initial_population_per_room: 32
  tools: [tell, mate, go]
  chat_format: true
  read_policy: drain
  observation_absorption: utterance
  mate_window_tokens: 64
  viability_probe_turns: 3

# Refill triggers on FREE BLOCKS, not head-count. Under the old head-count
# floor the rooms sat pinned at 8 agents while the pool was 92-94% full, so
# every immigrant was inserted into an almost-full room and brought everyone
# else's death forward; the two 4,000-block rooms needed 1,826 and 3,441
# refills on that treadmill while an 8,000-block room needed 30.
refill:
  enabled: true
  min_population: 0
  max_free_fraction: 0.40
  check_every_steps: 500
  max_per_check: 4
  takeoff_window_steps: 20000
  takeoff_min_births: 5

genome:
  rank: 16
  alpha: 32
  target_modules: [q_proj, k_proj, v_proj, o_proj]
  init_scale: 0.02
  mutation_std: 0.01
  crossover: chromosomal
  chromosomes: 3
  mutation: additive
engine:
  gpu_memory_utilization: 0.85
  max_loras: 8
  # Rooms now hold far more agents, so the CPU-side adapter cache has to hold
  # them: 256 x 20.2 MB is ~5 GB of host RAM.
  max_cpu_loras: 256
  max_num_seqs: 128
  enable_prefix_caching: true
  enable_chunked_prefill: true
  safety_margin_blocks: 128
  adapter_dir: /dev/shm/evollm

sampling:
  temperature: 1.0
  top_p: 1.0

run:
  max_world_steps: 400000
  snapshot_every_steps: 40000
  occupancy_every_steps: 1000
  trace_turns: 20000
  context_snapshot_every_steps: 25000
  context_snapshot_agents: 3
